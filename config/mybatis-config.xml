<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>

	<!-- 配置类的别名，使用批量的方式 -->
	<typeAliases>
		<package name="cn.wzd.ssm.po"/>
	</typeAliases>

	<mappers>
		<mapper resource="mybatisMapper/UserMapper.xml" />
		<!-- 使用MapperScannerConfigurer时，如果mapper.java和mapper.xml在相同路径下
		则可以不设置mapper。
		 -->
		<!-- <package name="cn.wzd.ssm.mapper.xml"/> -->
	</mappers>
</configuration>